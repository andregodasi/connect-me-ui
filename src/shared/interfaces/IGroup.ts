export interface GroupForm {
  id?: string;
  name: string;
  description: string;
  slug: string;
}

export interface Group {
  uuid: string;
  name: string;
  description: string;
  slug: string;
}
